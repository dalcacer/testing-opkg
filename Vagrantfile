# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|
  config.vm.box = "Adam/OpenWrt-AA"
  config.vm.provider "virtualbox" do |v|
      v.memory = 512
      v.cpus = 2
      v.gui = true
  end
end
